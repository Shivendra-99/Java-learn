// Update this if a custom domain is attached later (Vercel project settings),
// then regenerate public/sitemap.xml (npm run generate:sitemap).
export const SITE_URL = "https://java-learn.vercel.app"
export const SITE_NAME = "Java Learn"
export const DEFAULT_DESCRIPTION =
  "Learn Java for free with short, visual lessons — the JVM, objects, memory, collections, generics, exceptions, streams, lambdas, and concurrency, each with runnable examples and quizzes."
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-image.png`
